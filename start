#include <stdio.h>

// Write your sumArray function here

int main() {
    int n;
    scanf("%d", &n);
    
    int arr[n];
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    
    // Call the sumArray function and print the result
    
    return 0;
}
